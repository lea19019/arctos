# Smoke config — smallest model, tiny n, for validating the full path
# (quantize -> translate -> score) before launching the real sweep.
model:
  name: qwen3-1.7b-smoke
  hf_path: Qwen/Qwen3-1.7B
  chat_kwargs:
    enable_thinking: false
  max_new_tokens: 128
  batch_size: 8
